-------------------------------------------------------------------------------------------------------
1. Prepare Data and Move JSON Logic (Target: IHTC_Data.hh and IHTC_Data.cc)
-------------------------------------------------------------------------------------------------------
Context: We are working on the IHTP (Integrated Healthcare Timetabling Problem) in C++. I want to streamline my IHTC_Driver.cc and encapsulate the output logic.
CRITICAL RULE: In .hh header files, you must write ONLY the one-line function declarations. Do NOT write function bodies in .hh files. ALL implementations MUST go in the corresponding .cc source files.
Task 1: Open IHTC_Data.hh. Inside the IHTC_Output struct/class, add a flat data structure for nurse assignments: define struct NurseAssignment { int nurse_idx; int day; int shift; int room_idx; }; and add std::vector<NurseAssignment> nurse_assignments;. Then, add exactly these two one-line declarations: void writeJSON(const std::string& filename) const; and void printCosts() const;.
Task 2: Open IHTC_Data.cc. Implement printCosts() so it calculates and prints to the terminal all the Soft costs (Total, Unscheduled, Delay, OpenOT, AgeMix, Skill, Excess, Continuity, SurgeonTransfer).
Task 3: Still in IHTC_Data.cc, implement writeJSON(const std::string& filename) const. Move ALL the JSON building logic (nlohmann::ordered_json sol) currently located in IHTC_Driver.cc into this method. It must read the internal state of IHTC_Output and write the final file, strictly maintaining the exact JSON structure required by the validator.
-------------------------------------------------------------------------------------------------------
2. Implement Nurse Greedy Algorithm (Target: IHTC_Greedy.hh and IHTC_Greedy.cc)
-------------------------------------------------------------------------------------------------------
Context: My Greedy algorithm in IHTC_Greedy.cc solves PAS and SCP but ignores Nurse-to-Room Assignment (NRA).
CRITICAL RULES: > 1) The algorithm MUST remain strictly Greedy (constructive only, NO backtracking, NO local search, NO external solvers).
2) .hh files must contain ONLY one-line declarations. Implementations go in .cc.
Task 1: Open IHTC_Greedy.hh. Add ONLY the one-line declaration for a new helper function: void assignNursesGreedy(const IHTC_Input& in, IHTC_Output& out);.
Task 2: Open IHTC_Greedy.cc. Implement assignNursesGreedy. Iterate constructively: for each day, each shift, each room. If the room is occupied on that day/shift (Hard constraint H8), find the best available nurse.
Task 3: The "Greedy" choice must filter for nurses who are on shift (check roster). Choose the one that minimizes the impact on Soft constraints (e.g., lowest current workload to minimize S4, or closest skill to minimize S2). Save this in the nurse_assignments vector inside out.
Task 4: In IHTC_Greedy.cc, modify the main function GreedyIHTCSolver to call assignNursesGreedy(in, out); immediately after scheduleInOrder.
-------------------------------------------------------------------------------------------------------
3. Clean up the Driver (Target: IHTC_Driver.cc)
-------------------------------------------------------------------------------------------------------
Context: All JSON export logic is now in IHTC_Data.cc and the nurse assignment logic (NRA) is in IHTC_Greedy.cc. The driver file now contains dead code and disorganized logic.
Task: Open IHTC_Driver.cc. Delete all the content of the main function and rewrite it from scratch. It must be extremely lean and readable, strictly following this linear structure:

    1. Parse command-line arguments (input_file).

    2. Initialize IHTC_Input in(argv[1]); and handle loading errors.

    3. Initialize IHTC_Output out(in);.

    4. Print a very brief summary of the parsed data (number of patients, rooms, days).

    5. Call the solver engine: GreedyIHTCSolver(in, out);.

    6. Call the new output methods: out.printCosts(); and out.writeJSON("solution.json");.

    7. Return 0.

Strict requirement: No calculations, no json.push_back, and absolutely no algorithmic logic should remain in the driver file. It should only orchestrate the calls.